# Create a lambda function to replicate a consumer, 
# It will subscribe to the sqs queue and consume events from the queue
# It will import the db_sink and notification_sink modules & use them to save events to the database and send notifications

# Pranshu
